
#setup router

0)wget https://downloads.openwrt.org/chaos_calmer/15.05/ar71xx/generic/openwrt-15.05-ar71xx-generic-tl-mr3020-v1-squashfs-factory.bin

1) install e2fsprogs to format drive (two partitions) as ext4
# 
2) diskutil umount /dev/disk2s1 && umount /dev/disk2s2
#mkfs should also support setting up GUID table, look into it later
3) <homebrew_bin_location>mkfs.ext4 /dev/disk2s1 

#after setup router

mkdir /mnt/sda1
mount /dev/sda1 /mnt/sda1
tar -C /overlay -cvf - . | tar -C /mnt/sda1 -xf -
